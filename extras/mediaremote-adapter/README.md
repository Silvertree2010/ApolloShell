# mediaremote-adapter (eingebunden)

Origin: https://github.com/ungive/mediaremote-adapter, tag `v0.7.7` (commit `e3ff5021eb0875858bd05f48d2e9ba2e962d1cf6`, 2026-09-03), taken over unchanged: `src/`, `include/`, `bin/mediaremote-adapter.pl`, `LICENSE` (BSD-3-Clause, Copyright (c) 2025 Jonas van den Berg and contributors).

It is built without cmake by `scripts/build-mediaremote-adapter.sh` (which mirrors the tag's `CMakeLists.txt` with clang); `build.sh` puts the framework into `ApolloShell.app/Contents/Frameworks` and the Perl script into `Contents/Resources`.
